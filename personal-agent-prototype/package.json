{
  "name": "personal-agent-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check home-worker/daemon.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
